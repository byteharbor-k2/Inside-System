# course_5

主题：程序和进程。

这一讲围绕进程抽象、`procfs`、`fork()`、`execve()`、`_exit()` 等系统调用，以及基于进程快照的并行搜索示例展开。

示例代码来源：

- 课程主页中的第 5 讲页面：<https://jyywiki.cn/OS/2026/lect5.md>
- 下载时间：2026-04-04

已同步的示例目录：

- `crazy-os/`
- `proc-info/`
- `pstree/`
- `fork-dfs/`
- `fork-demo/`
- `execve-demo/`

说明：

- 以上目录来自页面中的 demo 链接 `/OS/demos/virtualization/...`。
- `fork-dfs/` 上游目录包含一个已编译的 `fork-dfs` 可执行文件，已一并保留。
- 页面中的若干内联代码块多为讲义片段或非完整程序，本次未额外整理成独立源码文件。
